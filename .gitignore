# `node_modules` (no trailing slash) so a symlink named node_modules is also
# ignored — a trailing-slash pattern only matches a directory, which let
# workspace node_modules symlinks slip into commits.
node_modules
dist/
*.log

# run-time generated
runs/
workspaces/
locks/
.harness/
backlog/
# `harness dashboard export` の生成物（DB read model から都度再生成）
docs/dashboard/index.html

# scratch
tmp/

# editor
.DS_Store

# local agent/tool state
.claude/
.codex/
